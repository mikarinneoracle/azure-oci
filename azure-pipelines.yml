# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: OCI-pool

steps:

- script: |
    export OCI_CLI_AUTH=instance_principal
    oci resource-manager job create-apply-job --stack-id ocid1.ormstack.oc1.eu-amsterdam-1.amaaaaaauevftmqac2ajso4kvixiuyajgyj3n7cljwkugaptdcyekrzumzva --execution-plan-strategy AUTO_APPROVED
  displayName: 'cli testing'
